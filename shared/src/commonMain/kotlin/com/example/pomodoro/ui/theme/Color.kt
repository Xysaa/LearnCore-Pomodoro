package com.example.pomodoro.ui.theme

import androidx.compose.ui.graphics.Color

// ── Deep Forest Night Palette ──────────────────────────────────────────────
val BackgroundDark       = Color(0xFF0D1F1A)   // Background utama
val SurfaceDark          = Color(0xFF152B23)   // Background card
val BorderGreen          = Color(0xFF1F3D2E)   // Border / separator

val AccentFocus          = Color(0xFF4ADE80)   // Hijau sage — fokus
val AccentBreak          = Color(0xFF2DD4BF)   // Teal — istirahat
val AccentWarning        = Color(0xFFFBBF24)   // Amber — udara sedang
val AccentDanger         = Color(0xFFF87171)   // Merah koral — udara buruk
val AccentOrange         = Color(0xFFF97316)   // Orange — udara tidak sehat

val TextPrimary          = Color(0xFFF0FDF4)   // Putih gading
val TextSecondary        = Color(0xFF86EFAC)   // Abu kehijauan

val RingBackground       = Color(0xFF1F3D2E)   // Latar cincin progress
val ButtonPrimaryStart   = Color(0xFF4ADE80)
val ButtonPrimaryEnd     = Color(0xFF2DD4BF)

// Status badge
val StatusPresent        = Color(0xFF4ADE80)
val StatusAbsent         = Color(0xFF6B7280)
val StatusError          = Color(0xFFF87171)

// AQI level colors
val AqiGood              = Color(0xFF4ADE80)
val AqiModerate          = Color(0xFFFBBF24)
val AqiUnhealthy         = Color(0xFFF97316)
val AqiHazardous         = Color(0xFFF87171)
